import React from 'react';
import { Users, Clock, Rocket, Tags, ExternalLink } from 'lucide-react';
import { Card, CardContent } from '@/components/ui/card';
import { Button } from '@/components/ui/button';
import { Badge } from '@/components/ui/badge';

interface App {
  id: number;
  name: string;
  description: string;
  launchDate: string;
  category: string;
  tags: string[];
  userCount: number;
}

interface AppCardProps {
  app: App;
}

const AppCard: React.FC<AppCardProps> = ({ app }) => {
  return (
    <Card className="group bg-white/95 backdrop-blur-sm border-0 shadow-xl hover:shadow-2xl transition-all duration-500 hover:scale-[1.02] overflow-hidden relative">
      {/* Animated background */}
      <div className="absolute inset-0 bg-gradient-to-br from-indigo-50 via-purple-50 to-pink-50 opacity-0 group-hover:opacity-100 transition-opacity duration-500" />

      <CardContent className="p-6 relative z-10">
        <div className="flex justify-between items-start mb-4">
          <div className="flex-1">
            <div className="flex items-center gap-2 mb-2">
              <h3 className="text-xl font-bold bg-gradient-to-r from-indigo-600 to-pink-600 bg-clip-text text-transparent group-hover:from-purple-600 group-hover:to-blue-600 transition-all duration-300">
                {app.name}
              </h3>
              <Rocket className="w-5 h-5 text-orange-500 opacity-0 group-hover:opacity-100 transition-opacity duration-300 animate-pulse" />
            </div>
            <Badge className="bg-gradient-to-r from-pink-500 to-indigo-600 text-white shadow-lg hover:shadow-xl transition-shadow duration-300">
              {app.category}
            </Badge>
          </div>
          <div className="text-right">
            <div className="text-sm font-semibold text-gray-700 mb-1">
              <Users className="inline-block w-4 h-4 mr-1 text-blue-500" />
              {app.userCount} users
            </div>
          </div>
        </div>

        <p className="text-gray-600 mb-4">{app.description}</p>

        <div className="space-y-3 mb-6">
          <div className="flex items-center text-gray-600 p-2 rounded-lg hover:bg-purple-50 transition-colors duration-300">
            <div className="w-8 h-8 bg-gradient-to-r from-purple-100 to-purple-200 rounded-full flex items-center justify-center mr-3">
              <Clock className="w-4 h-4 text-purple-600" />
            </div>
            <span className="font-medium">Launched: {app.launchDate}</span>
          </div>
          <div className="flex items-center text-gray-600 p-2 rounded-lg hover:bg-blue-50 transition-colors duration-300">
            <div className="w-8 h-8 bg-gradient-to-r from-blue-100 to-blue-200 rounded-full flex items-center justify-center mr-3">
              <Tags className="w-4 h-4 text-blue-600" />
            </div>
            <span className="font-medium space-x-1">
              {app.tags.map((tag, index) => (
                <Badge key={index} className="bg-blue-100 text-blue-700 mx-1">
                  {tag}
                </Badge>
              ))}
            </span>
          </div>
        </div>

        <div className="flex justify-end">
          <Button className="bg-gradient-to-r from-indigo-500 to-pink-600 hover:from-indigo-600 hover:to-pink-700 shadow-lg hover:shadow-xl transition-all duration-300 hover:scale-105 group/btn">
            <ExternalLink className="w-4 h-4 mr-2 group-hover/btn:rotate-12 transition-transform duration-300" />
            Open App
          </Button>
        </div>
      </CardContent>
    </Card>
  );
};

export default AppCard;
